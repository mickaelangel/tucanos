================================================================================
  COMMANDES POUR GITLAB SUR WINDOWS 11
================================================================================

📋 ÉTAPES RAPIDES
================================================================================

1️⃣ CRÉER LE PROJET SUR GITLAB (dans le navigateur)
────────────────────────────────────────────────────

   → Ouvrez GitLab dans votre navigateur
   → Cliquez "New project" → "Create blank project"
   → Nom: tucanos-suse15-offline
   → ❌ NE COCHEZ PAS "Initialize with README"
   → Cliquez "Create project"
   → Copiez l'URL qui s'affiche (exemple: https://gitlab.com/user/tucanos.git)


2️⃣ MÉTHODE AUTOMATIQUE (RECOMMANDÉ)
────────────────────────────────────────────────────

   Ouvrez PowerShell dans le dossier et exécutez:

   .\init-git-windows.ps1

   Le script vous guidera étape par étape !


3️⃣ MÉTHODE MANUELLE (PowerShell)
────────────────────────────────────────────────────

   Copiez-collez ces commandes UNE PAR UNE dans PowerShell:

   # Aller dans le bon dossier
   cd C:\Users\mickaelangel\Desktop\tucanos\tucanos\tucanos-suse15-ultimate-complete

   # Initialiser Git
   git init

   # Configurer votre identité (MODIFIEZ avec vos infos!)
   git config user.name "Votre Nom"
   git config user.email "votre.email@example.com"

   # Configuration Windows
   git config core.autocrlf true

   # Ajouter tous les fichiers
   git add .

   # Premier commit
   git commit -m "Initial commit: Package Tucanos SUSE 15 SP4"

   # Renommer la branche
   git branch -M main

   # Ajouter GitLab (REMPLACEZ par votre URL copiée à l'étape 1!)
   git remote add origin https://gitlab.com/votre-nom/tucanos-suse15-offline.git

   # Pousser vers GitLab
   git push -u origin main


4️⃣ AUTHENTIFICATION
────────────────────────────────────────────────────

   Lors du git push, GitLab demandera:

   → Username: votre nom d'utilisateur GitLab
   → Password: utilisez un TOKEN (PAS votre mot de passe!)

   📌 Créer un token:
      1. GitLab → Avatar (en haut à droite) → Settings
      2. Access Tokens → Add new token
      3. Nom: git-windows
      4. Scopes: cochez "read_repository" et "write_repository"
      5. Create → COPIEZ LE TOKEN
      6. Collez ce token comme "password"


================================================================================
  GESTION DES FICHIERS VOLUMINEUX (optionnel mais recommandé)
================================================================================

Si votre projet est trop gros pour GitLab, AVANT le "git add .", éditez .gitignore:

   notepad .gitignore

Ajoutez à la fin:

   # Exclure les fichiers volumineux
   vendor/
   dependencies/system/*.rpm
   dependencies/rust/*.tar.gz

Sauvegardez et fermez.

Ensuite continuez normalement avec git add, commit, push.


================================================================================
  VÉRIFICATION
================================================================================

Après le push, vérifiez:

   # Statut Git
   git status
   # Devrait afficher: "nothing to commit, working tree clean"

   # Dépôt distant configuré
   git remote -v
   # Devrait afficher votre URL GitLab

   # Dans le navigateur
   → Rafraîchissez votre page GitLab
   → Vous devriez voir tous vos fichiers!


================================================================================
  COMMANDES POUR PLUS TARD
================================================================================

📝 Modifier et pousser des changements:

   # 1. Modifier vos fichiers...

   # 2. Voir les changements
   git status

   # 3. Ajouter les modifications
   git add .

   # 4. Créer un commit
   git commit -m "Description de vos modifications"

   # 5. Pousser vers GitLab
   git push


📥 Cloner sur une autre machine:

   # Dans PowerShell, aller où vous voulez cloner
   cd C:\Users\VotreNom\Desktop

   # Cloner depuis GitLab
   git clone https://gitlab.com/votre-nom/tucanos-suse15-offline.git

   # Entrer dans le dossier
   cd tucanos-suse15-offline


================================================================================
  RÉSOLUTION DE PROBLÈMES
================================================================================

❌ "git n'est pas reconnu..."
   → Git n'est pas installé
   → Téléchargez: https://git-scm.com/download/win
   → Installez et redémarrez PowerShell

❌ "Permission denied (publickey)"
   → Utilisez HTTPS, pas SSH
   → OU configurez une clé SSH sur GitLab

❌ "Authentication failed"
   → Utilisez un TOKEN au lieu de votre mot de passe
   → Voir section "Authentification" ci-dessus

❌ "Updates were rejected..."
   → Faites: git pull origin main
   → Puis: git push

❌ Le push prend trop de temps
   → Votre projet est trop gros
   → Utilisez la section "Gestion des fichiers volumineux"


================================================================================
  LIENS UTILES
================================================================================

📚 Documentation complète:
   - GITLAB-WINDOWS-GUIDE.md    (dans ce dossier)
   - GIT-SETUP.md                (configuration générale)
   - GIT-COMMANDES-RAPIDES.txt   (aide-mémoire)

🌐 Ressources en ligne:
   - Git pour Windows: https://git-scm.com/download/win
   - Documentation GitLab: https://docs.gitlab.com/
   - Créer un token: https://docs.gitlab.com/ee/user/profile/personal_access_tokens.html


================================================================================
  RÉCAPITULATIF EXPRESS
================================================================================

Sur GitLab (navigateur):
   New project → tucanos-suse15-offline → Create → Copier l'URL

Sur Windows (PowerShell):
   cd C:\Users\mickaelangel\Desktop\tucanos\tucanos\tucanos-suse15-ultimate-complete
   .\init-git-windows.ps1

   OU manuellement:
   git init
   git config user.name "Votre Nom"
   git config user.email "votre@email.com"
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <URL_GITLAB>
   git push -u origin main

Authentification:
   Username: votre-nom-gitlab
   Password: COLLER_LE_TOKEN

Vérifier:
   Rafraîchir GitLab dans le navigateur → vos fichiers sont là!


================================================================================

🚀 Prêt à utiliser GitLab depuis Windows 11 !

Pour commencer: .\init-git-windows.ps1

================================================================================
